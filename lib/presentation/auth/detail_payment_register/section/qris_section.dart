import 'package:flutter/material.dart';

class QrisSection extends StatelessWidget {
  const QrisSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Qris'));
  }
}
