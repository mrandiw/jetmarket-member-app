import 'package:flutter/material.dart';

class RetailSection extends StatelessWidget {
  const RetailSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Retail'));
  }
}
