import 'package:flutter/widgets.dart';

class IBankingSection extends StatelessWidget {
  const IBankingSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
